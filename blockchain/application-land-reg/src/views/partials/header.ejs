<!doctype html>
<html lang="en">
    <head>
        <!-- Required meta tags -->
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <!-- Bootstrap CSS -->
        <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
        <title><%= title %></title>
    </head>
    <body>
        <% if (typeof show_navbar != 'undefined' && show_navbar) { %>
            <%- include("navbar.ejs") %>
        <% } %>